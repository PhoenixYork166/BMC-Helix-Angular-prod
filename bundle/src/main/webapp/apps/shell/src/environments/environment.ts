export const environment = {
  production: false,
  bundleId: 'com.example.test210500' // comment out this line if you don't want the code of other angular bundles to be loaded or
  //set bundleId to '' (empty string) if you want the code of your bundle to be loaded from the server
};
