export declare const FOCUSABLE_ELEMENTS: string[];
/**
 * The function returns all the focusable elements in the passed container
 * By default it is skip disabled elements
 *
 * Keep in mind that the disabled attribute can be used on the following elements:
 * - <button>
 * - <fieldset>
 * - <input>
 * - <optgroup>
 * - <option>
 * - <select>
 * - <textarea>
 */
export declare function getFocusableElements(element: ParentNode, skipDisabled?: boolean): HTMLElement[];
