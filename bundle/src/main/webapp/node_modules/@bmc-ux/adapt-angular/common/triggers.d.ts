import { Renderer2 } from '@angular/core';
export declare class Trigger {
    open: string;
    close?: string;
    constructor(open: string, close?: string);
    isManual(): boolean;
}
export declare function parseTriggers(triggers: string, aliases?: Record<string, string[]>): Trigger[];
export declare function listenToTriggers(renderer: Renderer2, nativeElement: unknown, triggers: string, openFn: any, closeFn: any, toggleFn: any): () => void;
