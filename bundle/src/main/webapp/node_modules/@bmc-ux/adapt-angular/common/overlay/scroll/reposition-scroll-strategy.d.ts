import { NgZone } from '@angular/core';
import { OverlayReference } from '@angular/cdk/overlay/overlay-reference';
import { RepositionScrollStrategyConfig, ScrollStrategy } from '@angular/cdk/overlay';
import { ViewportRuler } from '@angular/cdk/scrolling';
export declare class AdaptRepositionScrollStrategy implements ScrollStrategy {
    private _viewportRuler;
    private _ngZone;
    private _config?;
    private _scrollSubscription;
    private _overlayRef;
    private _overlayRefOriginIntersectObserver;
    constructor(_viewportRuler: ViewportRuler, _ngZone: NgZone, _config?: RepositionScrollStrategyConfig);
    /**
     * Attaches this scroll strategy to an overlay
     */
    attach(overlayRef: OverlayReference): void;
    /**
     * Enables repositioning of the attached overlay on scroll
     */
    enable(): void;
    /**
     * Disables repositioning of the attached overlay on scroll
     */
    disable(): void;
    /**
     * Detaches the scroll listener
     */
    detach(): void;
}
