import { ElementRef } from '@angular/core';
import { Directionality } from '@angular/cdk/bidi';
import { Point } from '@angular/cdk/drag-drop';
import { TooltipPlacement } from '../common/positioning';
import * as i0 from "@angular/core";
export interface PositionPointsData {
    point: Point;
    translatePoint: Point;
}
export declare class TooltipService {
    private elementRef;
    private dir;
    private _offset;
    private _arrowSize;
    constructor(elementRef: ElementRef, dir: Directionality);
    getActualPosition(x: number, y: number): Point;
    getFixedPosition(target: HTMLElement, placement: TooltipPlacement): PositionPointsData;
    getAutoPosition(target: HTMLElement): Promise<{
        point: Point;
        placement: string;
    }>;
    private getTopPosition;
    private getRightPosition;
    private getBottomPosition;
    private getLeftPosition;
    private getWidthHeight;
    static ɵfac: i0.ɵɵFactoryDeclaration<TooltipService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<TooltipService>;
}
