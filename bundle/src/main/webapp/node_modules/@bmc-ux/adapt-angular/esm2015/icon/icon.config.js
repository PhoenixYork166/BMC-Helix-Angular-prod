import { Injectable } from '@angular/core';
import { adaptWarn } from '../common/logger';
import * as i0 from "@angular/core";
export class AdaptIconConfig {
    constructor() {
        this._iconRegistry = new Map();
    }
    registerIcons(icons) {
        icons.forEach((icon) => {
            this._iconRegistry.set(icon.name, icon.data);
        });
    }
    getIcon(name) {
        const icon = this._iconRegistry.get(name) || '';
        if (!icon) {
            adaptWarn(`There is no registered icon with name: "${name}".
      Please make sure it has been added to register with AdaptIconConfig.registerIcons() method.`);
        }
        return this._iconRegistry.get(name);
    }
}
AdaptIconConfig.ɵfac = function AdaptIconConfig_Factory(t) { return new (t || AdaptIconConfig)(); };
AdaptIconConfig.ɵprov = /*@__PURE__*/ i0.ɵɵdefineInjectable({ token: AdaptIconConfig, factory: AdaptIconConfig.ɵfac, providedIn: 'root' });
(function () { (typeof ngDevMode === "undefined" || ngDevMode) && i0.ɵsetClassMetadata(AdaptIconConfig, [{
        type: Injectable,
        args: [{
                providedIn: 'root'
            }]
    }], null, null); })();
//# sourceMappingURL=icon.config.js.map