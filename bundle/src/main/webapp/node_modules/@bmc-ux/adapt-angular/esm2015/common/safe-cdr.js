/*
* This util class created to prevent issue described here - https://github.bmc.com/bmc-ux/adapt-angular/issues/3561
* */
export class SafeCdr {
    static detectChanges(cdr) {
        // TODO: Refactor this if possible
        /* eslint-disable-next-line @typescript-eslint/no-explicit-any */
        if (!cdr.destroyed) {
            cdr.detectChanges();
        }
    }
    static markForCheck(cdr) {
        // TODO: Refactor this if possible
        /* eslint-disable-next-line @typescript-eslint/no-explicit-any */
        if (!cdr.destroyed) {
            cdr.markForCheck();
        }
    }
}
//# sourceMappingURL=safe-cdr.js.map