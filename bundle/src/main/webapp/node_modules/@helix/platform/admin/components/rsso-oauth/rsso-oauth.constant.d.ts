import { IRssoOAuthSection } from './rsso-oauth.types';
export declare const RX_RSSO_OAUTH: {
    recordDefinitionName: string;
    oAuthConfigurationDescription: string;
    fieldIds: {
        bundleId: number;
        oAuthProvider: number;
    };
    sections: IRssoOAuthSection[];
};
