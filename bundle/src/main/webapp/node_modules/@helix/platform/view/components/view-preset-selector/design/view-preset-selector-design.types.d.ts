import { IRxAvailableOnDevicesProp } from '@helix/platform/view/api';
export interface IViewPresetSelectorDesignProperties extends IRxAvailableOnDevicesProp {
    name: string;
    styles: string;
    enableSharing: boolean;
}
