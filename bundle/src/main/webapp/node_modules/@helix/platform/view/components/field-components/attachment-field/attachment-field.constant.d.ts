import { UploaderMode, UploaderSelectionMode } from '@bmc-ux/adapt-angular';
export declare const RX_ATTACHMENT_FIELD: {
    defaultOptions: {
        filesCount: number;
        mode: UploaderMode;
        selectionMode: UploaderSelectionMode;
        chunkSize: string;
        maxFileSize: string;
    };
};
