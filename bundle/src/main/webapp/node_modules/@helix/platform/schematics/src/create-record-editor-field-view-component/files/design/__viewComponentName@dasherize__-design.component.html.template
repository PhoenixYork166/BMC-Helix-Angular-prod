<!--The values are inherited (model).-->
<!--In this example the "foo" field is a text field.-->
<p>The configured message will be displayed here.</p>

<adapt-rx-textfield [required]="model.isRequired$ | async"
                    [label]="model.label$ | async"
                    [disabled]="true"
                    ngModel
></adapt-rx-textfield>
