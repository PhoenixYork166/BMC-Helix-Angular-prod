import { OnInit, TemplateRef } from '@angular/core';
import { Router } from '@angular/router';
import { IRecordDefinition } from '@helix/platform/record/api';
import { RxRuleDefinitionDataPageService, RxRuleDefinitionService, RxRuleTriggerEventPipe } from '@helix/platform/rule/api';
import { IBundleDescriptor, IDataPageResult, RxBundleCacheService, RxCommandFactoryService, RxDefinitionNameService, RxGlobalCacheService, RxOverlayService } from '@helix/platform/shared/api';
import { RxModalService } from '@helix/platform/ui-kit';
import { IRowDataItem } from '@helix/platform/view/api';
import { IRecordGridColumn } from '@helix/platform/view/components';
import { TranslateService } from '@ngx-translate/core';
import { Observable } from 'rxjs';
import { IRenameAction } from '../bundle-definitions/bundle-definitions.types';
import { IDefinitionActionConfig } from '../bundle-details/bundle-details.types';
import { RxCopyDefinitionService } from '../copy-definition/copy-definition.service';
import { DefinitionTabComponent } from '../definition-tab/definition-tab.component';
import * as i0 from "@angular/core";
export declare class RuleDefinitionTabComponent implements OnInit {
    private router;
    private rxRuleTriggerEventPipe;
    private rxRuleDefinitionService;
    private rxOverlayService;
    private rxRuleDefinitionDataPageService;
    private rxModalService;
    private translateService;
    private rxCommandFactoryService;
    private rxBundleCacheService;
    private rxGlobalCacheService;
    private rxCopyDefinitionService;
    private rxDefinitionNameService;
    bundleDescriptor: IBundleDescriptor;
    definitionTabComponent: DefinitionTabComponent;
    definitionNameCellTemplate: TemplateRef<any>;
    private rowSelectionChanged$;
    private areNewDefinitionsAllowed;
    definitionActions$: Observable<IDefinitionActionConfig[]>;
    definitions$: Observable<IDataPageResult>;
    gridColumns: IRecordGridColumn[];
    isActionInProgress: boolean;
    recordDefinition: Partial<IRecordDefinition>;
    constructor(router: Router, rxRuleTriggerEventPipe: RxRuleTriggerEventPipe, rxRuleDefinitionService: RxRuleDefinitionService, rxOverlayService: RxOverlayService, rxRuleDefinitionDataPageService: RxRuleDefinitionDataPageService, rxModalService: RxModalService, translateService: TranslateService, rxCommandFactoryService: RxCommandFactoryService, rxBundleCacheService: RxBundleCacheService, rxGlobalCacheService: RxGlobalCacheService, rxCopyDefinitionService: RxCopyDefinitionService, rxDefinitionNameService: RxDefinitionNameService);
    private getDefinitionActions;
    ngOnInit(): void;
    onRenameDefinition({ selectedRow, definitionNames }: IRenameAction): void;
    onCopyDefinition(row: IRowDataItem): void;
    onDeleteDefinition(selectedRows: IRowDataItem[]): void;
    onRevertCustomization(selectedRows: IRowDataItem[]): void;
    onAddDefinition(): void;
    onRowSelectionChanged(selectedRows: IRowDataItem[]): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<RuleDefinitionTabComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<RuleDefinitionTabComponent, "ax-rule-definition-tab", never, { "bundleDescriptor": "bundleDescriptor"; }, {}, never, never>;
}
