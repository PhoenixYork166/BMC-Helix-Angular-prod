import { IViewActionDesignProperties } from '@helix/platform/view/api';

// This Interface will be used during runtime.
export interface I<%= classify(actionName) %>ActionProperties {
  message: string;
}

// This Interface will be used during design time.
export interface I<%= classify(actionName) %>ActionDesignProperties extends IViewActionDesignProperties{
  message: string;
}
